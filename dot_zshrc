# The following lines were added by compinstall
zstyle :compinstall filename '~/.zshrc'

autoload -Uz compinit
compinit
# End of lines added by compinstall
# Lines configured by zsh-newuser-install
HISTFILE=~/.histfile
HISTSIZE=100000000000
SAVEHIST=50000
setopt autocd notify
unsetopt beep
bindkey -e
# End of lines configured by zsh-newuser-install

## My config :^)
# Get colour output
alias ls="ls --color=auto"
alias grep="grep --color=auto"

# TODO: Update these to use chezmoi
# Editor shortcuts - these go into the main file
# alias kconf="$EDITOR ~/.config/kitty/kitty.conf"
# alias aconf="$EDITOR ~/.config/awesome/rc.lua"

# Editor shortcuts - these go into the folder
# alias kdconf="$EDITOR ~/.config/kitty/"
# alias adconf="$EDITOR ~/.config/awesome/"

# My cool prompt
prompt="%F{red}%B[%T]%b%f %F{cyan}%n@%m%f %F{cyan}%B%2~%b%f%# "

# Finally, print out the current todo
cat ~/.todo
