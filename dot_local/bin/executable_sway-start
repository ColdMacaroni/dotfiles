#!/usr/bin/env sh
# My little sway start script.
# It sets all the environment variables.

export XCURSOR_SIZE=16
export QT_STYLE_OVERRIDE=kvantum
export EDITOR=nvim
export GTK_THEME=Materia-dark-compact

# Needs env to preserve the variables
exec env sway "$@"
